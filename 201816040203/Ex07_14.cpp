#include <iostream>
#include<iomanip>
#include<vector>
#include<stdexcept>
using namespace std;
int main()
{
    vector<int>n(20);//20-element vector<int>
    int flag=0,i=0,j=0;
    for(size_t i=0;i<n.size();++i)
   {
       if(n[i]>=10&&n<=100)//判断区间
       {
           if(n[i]!=n[i])//判断相等
            flag==1;
            continue;
       }
       if(flag==1)
       {
           cout<<endl;
       }
   }//output value
   for (size_t j=0; intls[j]!=1&&j<intls.size(); ++j) 
    {
        if(flag=0)
         n.push_back(a);
                cout << intls[j];
                cout << "  ";
                }
    return 0;
}
